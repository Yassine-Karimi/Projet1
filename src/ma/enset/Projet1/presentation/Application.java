package ma.enset.tp_jdbc.presentation;

public class Application {
    public static void main(String[] args) {
        /*
        ProduitServiceImpl psi = new ProduitServiceImpl(new ProduitDaoImpl());
        /*Produit pr = new Produit();
        pr.setNom("ASUS");
        pr.setDescription("XR4595");
        pr.setPrix(9500);
        pr.setQuantite(47);
        psi.addProduit(pr);
        */
/*
        List<Produit> produits = psi.getProduitParMC("d");

        for (Produit p:produits) {
            System.out.println(p.toString());
        }

        /*
            List<Produit> produits = psi.getProduitParMC("as");
            for (Produit p:produits) {
                psi.deleteProduit(p);
            }
         */
    }
}
